name: madares_app_teacher
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+2
#version: 2.7.2+7

environment:
  sdk: ">=3.2.3 <4.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_bloc: ^8.1.6
  flutter_svg: ^2.0.10+1
  google_fonts: ^6.2.1
  dotted_border: ^2.1.0
  lottie: ^3.1.2
  dio: ^5.5.0+1
  showcaseview: ^3.0.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  table_calendar: ^3.1.2
  firebase_core: ^3.2.0
  firebase_messaging: ^15.0.3
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0
  connectivity_plus: ^6.1.1
  internet_connection_checker: ^1.0.0+1
  permission_handler: ^11.3.1
  url_launcher: ^6.3.0
  flutter_html:
  timeago: ^3.7.0

  package_info_plus: ^8.0.0
  file_picker: ^9.0.0
  open_file_manager:
  flutter_animate: ^4.5.0
  shared_preferences: ^2.2.3
  flutter_pdfview: ^1.3.2
  readmore: ^3.0.0
  image_picker: ^1.1.2
  any_link_preview: ^3.0.2
  flutter_keyboard_visibility: ^6.0.0
  flex_color_scheme: ^8.0.2
  record: ^5.1.2
  just_audio: ^0.9.39
  path:
  path_provider:

  flutter_launcher_icons:
  flutter_localization:
  # awesome_notifications: ^0.10.1
  flutter_local_notifications: ^18.0.1


dependency_overrides:
  http: 1.3.0
  intl: 0.20.2

dev_dependencies:
  hive_generator: ^2.0.1
  build_runner: ^2.1.0
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.3

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  assets:
    - assets/
    - assets/images/
    - google_fonts/
    - assets/languages/
    - assets/animations/
    - assets/new_school.jpeg
    - assets/palms_school.jpeg
    - assets/altuwaysh.jpeg

flutter_launcher_icons:
  android: true
  ios: true
  min_sdk_android: 23
  adaptive_icon_background: "#ffffff"
  adaptive_icon_foreground: "assets/altuwaysh.jpeg"
  image_path: "assets/altuwaysh.jpeg"

  flavors:
    school1:
      image_path: "assets/palms_school.jpeg"
    school2:
      image_path: "assets/new_school.jpeg"
    school3:
      image_path: "assets/altuwaysh.jpeg"